

function clicou() {

    let n = (window.prompt('Digite um número inteiro qualquer'))
  
    let a = n - 1
    let s = n * 1 + 1


    let = (window.alert(`Antes de ${n} temos o número ${a}.
Depois de ${n} temos o número ${s}`))
}